# Publish Checklist

- Run `npm run build`.
- Run `npm run lint`.
- Run `npm run test:e2e`.
- Open the editor locally and verify desktop and mobile canvas views.
- Press Download and inspect the exported ZIP.
